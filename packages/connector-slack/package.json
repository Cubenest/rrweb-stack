{
  "name": "@peekdev/connector-slack",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "Slack surface adapter + runnable for the peek connector platform (Socket Mode; Approve/Deny consent cards).",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "peek-connector-slack": "./dist/index.js" },
  "main": "./dist/index.js",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/postbuild.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.106.0",
    "@peekdev/connector-core": "workspace:*",
    "@slack/bolt": "^4.2.0",
    "@slack/types": "^2.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  },
  "engines": { "node": ">=22" }
}
