// Mirror of `@tracelane/wdio/hooks` so `wdio-tracelane-service/hooks` is a faithful
// alias for setups that wire the hook factory instead of registering the service.
export * from '@tracelane/wdio/hooks';
