---
'@tracelane/report': minor
---

Time-synced console and network panels — entries now reveal in sync with rrweb-player playback time instead of all appearing at page load. Click any row to seek the player to that moment. Tab badges show `current/total` so you can see at a glance how much of the recording you've watched. Filters compose with time-sync (a row is visible only when both conditions pass).
