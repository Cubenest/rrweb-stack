---
"@peekdev/cli": minor
---

Add `peek sessions search` (--q / --origin / --since / --until / --status / --errors / --limit / --json) to search recorded sessions by metadata + facets. Skill updated for the new `search_sessions` tool.
