---
"@peekdev/cli": patch
---

docs(cli): correct the supported-clients list (remove Continue/Zed, which `peek init` does not detect), fix the Cursor config path to the cross-platform `~/.cursor/mcp.json`, and add a Windows `~`-home-directory note to the client table.
