# QA findings — 2026-05-28

Phase 4c QA walk. Run against published alphas: `@tracelane/wdio@0.1.0-alpha.2`,
`@peekdev/cli@0.1.0-alpha.2`, `@peekdev/mcp@0.1.0-alpha.2`.

Environment: macOS Darwin 25.5.0, Node 24.4.1, pnpm 9.14.4, Chrome 148 stable.

QA workspace: `[qa-workspace]/`

*Fresh setup run after republish of all packages to `0.1.0-alpha.2`.*

---

## tracelane checklist status

| # | Item | Status | Notes |
|---|---|---|---|
| A.1 | Packages resolve | ✅ | `pnpm install` clean — no `workspace:*` errors. All deps at `0.1.0-alpha.2`. |
| A.2 | `wdio.conf.ts` parses | 🟡 | TS error still present in published `0.1.0-alpha.2`: `TraceLaneService` not assignable to `ServiceClass` (T-4). Workaround: `// @ts-expect-error`. Zero errors with workaround. |
| A.3 | Allure dev-dep optional | ⏸ | Not installed; Group D skipped. |
| B.1 | Passing spec writes nothing | ✅ | `tracelane-reports/` absent after green run. |
| B.2 | No state leaked outside outDir | ✅ | `~/.tracelane` does not exist. |
| C.1 | Failing spec writes a report | ✅ | One `.html` in `tracelane-reports/`. Filename format correct. |
| C.2 | Size cap | ✅ | 160 KB — well under 25 MB. |
| C.3 | Self-loading | ✅ | Report opens in Chrome with no broken assets. |
| C.4 | Replay works + panels | 🔴 | "Not enough recorded events to replay." rrweb captured nothing. Console: "No console output captured." Network: "No failed network requests captured." See T-9. |
| C.5 | Password masking | ⏸ | Blocked by C.4 — no replay to scrub. |
| C.6 | Offline | ⏸ | Not yet tested. |
| D.1–D.4 | Allure attach | ⏸ | Skipped (A.3 skipped). |
| E.1 | Firefox graceful degrade | ⏸ | Not tested. |
| E.2 | Safari | ⏸ | Not tested. |
| E.3 | WDIO 9 | ✅ | `webdriverio@9.27.2` installed. |
| F.1–F.3 | Edge cases | ⏸ | Not tested. |

---

## peek checklist status

| # | Item | Status | Notes |
|---|---|---|---|
| A.1 | MCP server starts + answers `initialize` | ✅ | Response received. `serverInfo.name = "peek-mcp"` (QA doc says `"peek"` — P-6). `version = "0.1.0-alpha.2"` ✅. |
| A.2 | Tool list has 10 expected names | ✅ | All 10 tools present and match the QA doc list exactly. |
| A.3 | `--version` flag | ⏸ | By design — flag does nothing. |
| A.4 | Native-host install via CLI | ⏸ | By design — install runs via `peek init`. |
| A.5 | Manifest landing location | ✅ | `com.cubenest.peek.json` exists at `~/Library/Application Support/Google/Chrome/NativeMessagingHosts/`. (QA doc says `com.peekdev.host.json` — P-5 doc bug.) |
| A.6 | Manifest content | 🔴 | `allowed_origins: []` — empty. Chrome blocks native messaging without extension ID. Root cause of all "no sessions" failures. See P-10. |
| B.1 | Install and PATH | 🟡 | `npm install -g @peekdev/cli` ✅. `which peek` ✅. `peek --version` = `0.1.0-alpha.0` — stale literal (P-8). |
| B.2 | `peek status` baseline (pre-init) | ✅ | Correct output. No crash. |
| B.3 | `peek init` configures Claude Code | ✅ | Detected Claude Code + Cursor. Wrote both MCP configs. `mcpServers.peek = { command: "npx", args: ["-y", "@peekdev/mcp"] }` ✅. |
| B.4 | `peek init` idempotent | ✅ | Second run said "Updated" (not "Wrote") for both clients. No JSON corruption. |
| B.5 | Multi-client | ✅ | Auto-detected Cursor. Offered VS Code and Windsurf. Wrote Cursor config. |
| B.6 | `peek sessions list` (empty) | ✅ | "No sessions recorded yet." |
| B.7 | `peek audit log` (empty) | ✅ | "No audit log yet." |
| C.1 | Extension build | ✅ | `.output/chrome-mv3/` built, 193 KB. IIFE assertion passes. |
| C.2–C.5 | Extension load, permissions, side panel | ⏸ | Not documented from this run. |
| D.1 | Initial idle | ⏸ | Not documented. |
| D.2 | Enable on this site | 🟡 | Side panel counters visible but only update on page reload (not live). See P-11. |
| D.3 | Counters increment | 🟡 | Increment on reload only — not on interaction. See P-11. |
| D.4 | Cross-origin not auto-recorded | ⏸ | Not tested. |
| E.1 | `peek status` shows non-zero | 🔴 | `sessions: 0` after active browsing. Root cause: P-10 (`allowed_origins: []`). |
| E.2 | `peek sessions list` shows session | 🔴 | "No sessions recorded yet." — blocked by P-10. |
| E.3 | `peek sessions show <id>` | 🔴 | No sessions to show — blocked by P-10. |
| E.4 | DB direct inspect | 🔴 | `started_at` column does not exist (see P-12 schema mismatch). `events_chunks` count: 0. |
| E.5 | events_chunks non-empty | 🔴 | Count: 0 — blocked by P-10. |
| E.6 | Chunk blobs on disk | 🔴 | `~/.peek/rrweb-events/` empty — blocked by P-10. |
| E.7 | console_events table | 🔴 | Count: 0 — blocked by P-10. |
| F.1–F.4 | Permission model | ⏸ | Requires live session. |
| F.5 | Level 4 real AI write | ⏸ | Phase-5-stub. |
| G.1 | Destructive unit test | ✅ | 32/32 pass. All ADR-0010 terms covered, policy delta tests pass. |
| G.2 | SW console dispatch | ⏸ | Requires extension. |
| H.1–H.7 | Deep capture | ⏸ | Requires live session. |
| I.1–I.4 | Privacy posture | ⏸ | Requires live session. |
| J.0 | peek shows up in Claude Code tools | ✅ | Claude Code picks up the MCP entry. All 10 tools available. |
| J.1 | "What's in my latest session?" | 🔴 | Claude correctly calls `list_recent_sessions` but returns empty — no sessions recorded (P-10). |
| J.2 | Console errors | 🔴 | Tool call succeeds, result empty — P-10. |
| J.3 | Network failures | 🔴 | Tool call succeeds, result empty — P-10. |
| J.4 | Action before error | 🔴 | Tool call succeeds, result empty — P-10. |
| J.5 | Generate Playwright repro | 🔴 | No sessions to generate from — P-10. |
| J.6 | DOM snapshot | 🔴 | No sessions — P-10. |
| J.7 | Write-class tool gating | ✅ | Claude correctly identifies Submit as high-impact, explains destructive-action override and missing session requirement. Graceful — no retry loop. |
| J.8 | Run generated repro | ⏸ | Blocked by J.5. |
| J.9 | MCP roots scoping | ⏸ | Not tested. |
| K.1–K.5 | CLI exports/deletes | 🔴 | `peek sessions export` fails: "missing <session-id>" — no sessions to export (P-10). |
| L.1–L.4 | Failure modes | ⏸ | Requires live session. |

---

## Findings detail

### T-1 / P-1 ✅ RESOLVED — `workspace:*` publish bug

Fixed in `0.1.0-alpha.2`. `pnpm install` and `npm install -g @peekdev/cli` both succeed cleanly.

---

### P-2 ✅ RESOLVED — `@peekdev/mcp` postinstall script not in `files`

Fixed in `0.1.0-alpha.2`. Install runs postinstall without error.

---

### P-3 ✅ RESOLVED — `peek` CLI silent under pnpm bin shim

Fixed in `0.1.0-alpha.2`. All CLI commands produce correct output.

---

### P-10 ✅ RESOLVED (alpha.3) — `allowed_origins: []` in native host manifest — blocks all recording

Resolved by prompting for the unpacked extension ID during `peek init` and
overriding `extensionIds.dev` before `buildManifest()` runs (see
`packages/peek-cli/src/commands/init.ts`). The validator
(`packages/peek-cli/src/lib/extension-id.ts`) pins the 32-char a–p shape; an
empty submission is allowed (skip — only useful with a populated CWS slot).

Original report below:

**Observed:** After `peek init`, the written manifest has an empty `allowed_origins`:

```json
{
  "name": "com.cubenest.peek",
  "description": "peek local bridge — native messaging host for the peek browser companion",
  "path": "/opt/homebrew/lib/node_modules/@peekdev/cli/node_modules/@peekdev/mcp/dist/index.js",
  "type": "stdio",
  "allowed_origins": []
}
```

**Impact:** Chrome enforces that `allowed_origins` contains the extension's Chrome
Web Store ID (or unpacked dev ID) before allowing a native messaging connection. With
an empty array, Chrome silently blocks all `chrome.runtime.connectNative()` calls from
the extension. The extension's SW runs but cannot reach the native host → no events
are written to `~/.peek/sessions.db`. This is the root cause of all E.* and J.1–J.6
failures.

**Why counters update on reload only:** The extension buffers events locally in the
content script / SW. On page reload the tab's state resets, incrementing the counter.
But the flush to the native host is blocked, so nothing persists.

**Root cause in source:** `installManifests()` in
`packages/peek-mcp/src/native-host/installer.ts` writes `allowed_origins` but the
extension ID is not available at `peek init` time (the extension is loaded unpacked
and gets a locally-assigned ID that changes per machine). The correct approach is to
either:
1. Prompt the user for the unpacked extension ID during `peek init`, or
2. Populate `allowed_origins` with a known set of IDs (store ID + common dev IDs), or
3. Use the extension's `externally_connectable` manifest key as an alternative channel.

**Fix:** `peek init` must write a non-empty `allowed_origins`. At minimum it should
include the known dev extension IDs from `src/native-host/extension-ids.json`.

---

### T-9 ✅ RESOLVED (alpha.3) — rrweb recording empty: "Not enough recorded events to replay"

**Root cause:** the Service + hooks-factory were calling `session.onUrl` from
`beforeCommand`. WDIO fires `beforeCommand` BEFORE the actual command runs, so
the recorder was being re-injected on a page that was about to be torn down by
the navigation. The new page loaded with no recorder, no in-page buffer, and
the per-test poll harvested 0 events. Verified by running the e2e smoke spec
with `logLevel: 'info'` — `tracelaneInitScript` and `injectBundleScript` ran
once on `about:blank` (the pre-navigation page), then never again on the
loaded fixture.

**Fix:** moved the re-injection hook from `beforeCommand` to `afterCommand`
(see `packages/tracelane-wdio/src/service.ts` and
`packages/tracelane-wdio/src/hooks.ts`). `afterCommand` fires after WDIO
returns from `url()`, by which point Chrome has navigated and the new
document is loaded — `reinject()` now lands rrweb on the actual page under
test. Verified by decoding the e2e report blob: 15 events captured (Meta,
FullSnapshot, 9 interaction Incrementals, plus tracelane.nav boundaries).
Errors from the `url()` command itself skip re-injection (the navigation
never happened, so nothing changed).

Original report below:

---

### T-4 ✅ RESOLVED (alpha.3) — `TraceLaneService` TypeScript type error

The alpha.2 intersection fix was insufficient — `Services.ServiceClass`
requires the constructor's 2nd and 3rd params to be compatible with
`Capabilities.ResolvedTestrunnerCapabilities` and `Options.Testrunner`
respectively. The previous `_capabilities?: unknown` + `config?: { framework?:
string }` were too narrow.

**Fix:** widened the constructor signature to use the WDIO interface's exact
types (see `packages/tracelane-wdio/src/service.ts`). Added a typecheck-only
test (`test/service-typecheck.test.ts`) that asserts `TraceLaneService` is
assignable to `Services.ServiceClass` so a future regression fails at `tsc`
time, not at the user's wdio.conf.ts.

---

### P-5 ✅ RESOLVED (alpha.2 / `384a630`) — QA doc specifies wrong native-host manifest name

The QA doc was updated to `com.cubenest.peek` in commit `384a630` — this row
in the maintainer's findings is stale.

---

### P-6 ✅ RESOLVED (alpha.2 / `384a630`) — `serverInfo.name` mismatch

The QA doc was updated to expect `serverInfo.name = "peek-mcp"` in commit
`384a630` — this row in the maintainer's findings is stale.

---

### P-8 ✅ RESOLVED (alpha.3) — `peek --version` shows stale version literal

**Fix:** replaced the hardcoded literal in `packages/peek-cli/src/version.ts`
with a runtime `createRequire(import.meta.url)('../package.json').version`
lookup, mirroring the alpha.2 fix to peek-mcp's `SERVER_VERSION`. Added a
regression test (`test/version.test.ts`) that asserts
`CLI_VERSION === pkg.version` so a future revert fails the suite.

---

### P-11 ✅ RESOLVED (alpha.3) — Side-panel event counters only update on page reload

**Root cause:** the SW only injected the MAIN-world recorder from
`chrome.tabs.onUpdated` with `status === 'loading'`. When the user clicked
"Enable for this site", `addEnabledOrigin()` persisted consent but no
existing tab got a recorder until the next reload — so counters stayed at
zero (in fact no events flowed to the SW at all; the on-reload bump was the
relay catching the page's initial DOM mutation burst, not user interaction).

**Fix:** added a `chrome.storage.onChanged` branch in
`packages/peek-extension/entrypoints/background.ts` that diff-checks the
`peek:enabledOrigins` key, queries every currently-open tab matching each
newly-added origin via `chrome.tabs.query({ url: '<origin>/*' })`, and runs
`maybeInject` on each. The `diffAddedOrigins` helper
(`packages/peek-extension/src/activation/storage.ts`) is the pure half;
unit-tested against junk payloads.

Original report below:

**Observed:** After enabling recording on a site, the event counters in the peek side
panel do not increment on user interaction (clicks, scrolls, input). They only jump
when the page is reloaded.

**Expected per D.3:** Counters should rise as the user interacts.

**Likely cause:** The content script or SW is batching events but the side panel UI
is not receiving live updates — either the `port.postMessage` from the content script
to the side panel is not firing on interaction, or the side panel's listener is only
wired to the `onActivated` / `onUpdated` tab events (which fire on reload).

**Note:** Even if the UI were fixed, data still can't flush to the DB due to P-10
(`allowed_origins: []`). Both bugs need fixing for E.* to pass.

---

### P-12 ✅ RESOLVED (alpha.3) — QA doc uses wrong SQLite column name (`started_at`)

The actual `sessions` schema has `created_at`, not `started_at`. The QA doc
query at E.3 / E.4 has been updated to use `created_at`.

Original report below:

The QA doc E.4 query:
```sql
SELECT id, url, started_at FROM sessions ORDER BY started_at DESC LIMIT 5;
```

Actual error: `no such column: started_at`. The sessions table schema (v2) uses a
different column name. Run `.schema sessions` to get the actual column name and update
the QA doc query.

---

### T-7 🟡 OPEN — Network capture unavailable in tracelane fixture

Both specs log `network capture unavailable (CDP not attached); degrading to rrweb+console only.`
C.4 network panel is empty. Unchanged.

---

## Findings summary

| Bucket | Count | Items |
|---|---|---|
| ✅ Pass | 17 | tracelane: A.1, B.1, B.2, C.1–C.3, E.3; peek: A.1, A.2, A.5, B.2–B.7, C.1, G.1, J.0, J.7 |
| 🔴 Showstopper | 2 | P-10 (`allowed_origins: []` — all recording blocked), T-9 (rrweb replay empty) |
| 🟡 Annoyance | 7 | T-4, T-7, P-5, P-6, P-8, P-11, P-12 |
| 🟢 Polish | 0 | — |
| ⏸ Blocked / Phase-5-stub | remainder | H, I, L, J.8–J.9, F.5, C.5–C.6 and others |

**Top showstoppers (must fix before public flip):**

1. P-10 — `allowed_origins: []`: the native host manifest written by `peek init` has
   no extension IDs → Chrome blocks native messaging → zero sessions recorded →
   all recording pipeline (E.*), all MCP session queries (J.1–J.6), and all CLI
   session operations (K.*) fail.
2. T-9 — rrweb replay empty: the tracelane headline feature produces a blank report.

**Top annoyances (fix this week):**

1. P-11 — Counters only update on reload (UX gap even before P-10 fix)
2. P-8 — `peek --version` shows wrong version
3. T-4 — TS type error in wdio.conf.ts blocks clean A.2
4. P-12 — Wrong column name in QA doc E.4 query
5. T-7 — Network panel empty in tracelane fixture

**Resolved since last run:**

- T-1/P-1 ✅ `workspace:*` publish bug
- P-2 ✅ postinstall script missing from `files`
- P-3 ✅ `peek` CLI silent under pnpm

**Overall verdict:** ☐ Ship · ☑ Hold for fixes · ☐ Hold for design review

2 showstoppers remain. Fix P-10 (populate `allowed_origins` from
`extension-ids.json`) and T-9 (diagnose rrweb injection in headless Chrome), then
re-walk E.*, J.1–J.6, K.* and C.4–C.5.
